<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.travel">


	<select id="selectPlaceList" 
		resultType="place">  <!-- 중복 관광지 제외 페이징 처리할 관광지 리스트 -->

		SELECT p.place_name as place_name,p.area_name as
		area_name,d.detail_cd_name as detail_cd_name

		FROM(
		SELECT ROW_NUMBER() OVER(PARTITION BY place.place_name ORDER BY
		place.area_name ) AS RNUM, place.*
		FROM place ) p,detail_code d

		WHERE RNUM = 1 and p.category=d.detail_cd_id


	</select>

</mapper>